```cpp
#include <vector>

vector<podatkovni_tip> ime_vektorja; //prazen vektor
vector<podatkovni_tip> ime_vektorja(velikost);

vector<int> a[10]; //10 praznih vektorjev
vector<int> a(10); //vektor z dolzino 10
vector<int> a(velikost, vrednost);

for(int i = 0; i < vec.size(), i++){
  cout << vec.at(i); //vec[i]
}
//for(auto i : vec)
vector<int> Vektor(Vektor2);
vector<int>vek2(vektor.begin(), vektor.end());

vec.resize(15);
vec.reserve(15);
vec.size() = 15;
vec.capacity() = 20;
